# Macro Example

The macro pass runs before the chunk pass.

%def(println_line, text, %{println!("%(text)");%})

```rust
// <[@file src/main.rs]>=
fn main() {
    %println_line(generated by a macro)
}
// @
```
